using Microsoft.UI.Xaml.Controls;

namespace Trackify.Presentation.Components;

/// <summary>Selected-segment inspector (drive behaviour + sensor). Binds to the hosting SecondViewModel.</summary>
public sealed partial class SegmentInspector : UserControl
{
    public SegmentInspector() => this.InitializeComponent();
}
