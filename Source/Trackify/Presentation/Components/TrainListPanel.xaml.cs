using Microsoft.UI.Xaml.Controls;

namespace Trackify.Presentation.Components;

/// <summary>Train list rail (search, discovery, filter, list, add). Binds to the hosting MainViewModel.</summary>
public sealed partial class TrainListPanel : UserControl
{
    public TrainListPanel() => this.InitializeComponent();
}
